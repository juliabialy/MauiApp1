<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Entry x:Name="InputBox" Grid.Row="0" WidthRequest="250" HeightRequest="25" Margin="0,0,0,10"/>
        <Button Text="Dodaj" Grid.Row="1" WidthRequest="100" HeightRequest="30" Margin="-130,0,0,0" Clicked="Dodaj_Clicked"/>
        <Button Text="Usuń ostatni" Grid.Row="1" WidthRequest="100" HeightRequest="30" Margin="160,0,0,0" Clicked="Usun_Clicked"/>

        
        <CollectionView x:Name="Lista" Grid.Row="2" Margin="0,10,0,0">
            <CollectionView.ItemTemplate>
                
                <DataTemplate>
                    <Label Text="{Binding .}" />
                </DataTemplate>
                
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>

</ContentPage>
